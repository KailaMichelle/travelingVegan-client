import React from 'react'

function Profile(){
    // const { user } = props;
    // console.log(user);
    return (
        <div>
            Profile
            {/* {user.username} */}
        </div>
    )
}

export default Profile;
